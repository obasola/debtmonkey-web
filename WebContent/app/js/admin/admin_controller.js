'use strict';

/**
 * Controller for Account
 **/
adminModule.controller('AdminCtrl', ['Account',  'AccountAddress', 'UserAccount', 'AccountType', '$scope', '$routeParams', 
                                     '$http', '$location', '$cookies', 'MessageHandler', 'restURL', 
                                     function(Account, AccountAddress, UserAccount, AccountType, $scope, $routeParams, $http, $location, 
                                    		 $cookies, MessageHandler, restURL) {
	 
    $scope.mode = null;


    /**
     * add functionss here
     */
	
   
}]);
